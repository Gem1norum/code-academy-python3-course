# Your code below:
toppings = ["pepperoni", "pineapple", "cheese", "sausage", "olives", "anchovies", "mushrooms"]

prices = [2, 6, 1, 3, 2, 7, 2]

num_two_dollar_slices = prices.count(2) # I count the occurances of the number 2 in the prices list
print(num_two_dollar_slices)

num_pizzas = len(toppings) # I find the length of the toppings list

print("We sell " + str(num_pizzas) + " different kinds of pizza!")

pizza_and_prices = [[2, "pepperoni"], [6, "pineapple"], [1, "cheese"], [3, "sausage"], [2, "olives"], [7, "anchovies"], [2, "mushrooms"]]

# print(pizza_and_prices)

pizza_and_prices.sort()

# print(pizza_and_prices)

cheapest_pizza = pizza_and_prices[0] # retrieving the first item of the list pizza_and_prices, and assigining it to this variable

priciest_pizza = pizza_and_prices[-1] # retrieving the last item of the list pizza_and_prices, and assigining it to this variable

pizza_and_prices.pop(-1) # removing the last item of the list pizza_and_prices

print(pizza_and_prices)

pizza_and_prices.append([2.5, "peppers"]) # here I add a new menu item and its price

pizza_and_prices.sort() # i resort the code here in ascending order of price, since the new addition is not ordered

print(pizza_and_prices)

three_cheapest = pizza_and_prices[:3] # here I extract the first three, i.e. the cheapest, list items

print(three_cheapest)
